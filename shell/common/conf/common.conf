# common.config - basic config file for scripts

# Set strict mode for safer scripting
set -euo pipefail
IFS=$'\n\t'

# Source additional configs if needed
# [ -f "$CONFIG_DIR/extra.conf" ] && source "$CONFIG_DIR/extra.conf"
